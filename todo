Secret management
Firewall block all incoming connections unless allowed I don't care what firewall just securely:
  Yggdrasil
  i2p
  reticulum?

figure out how to open yazi from the launcher since its in the list
  This works with app, but not search

workstation:
  mullvad
  gui:
    otter-launcher
      allow "git pull && NH_ELEVATION_STRATEGY=pkexec nh os switch" and throw error if failed with urgent notification and copy error
      git pull && NH_ELEVATION_STRATEGY=pkexec nh os switch --update
    switch out options such as easyeffects to mangoeasyeffects so I can swap wm's easier
    fix sunshine not registering keys or mouse moves
    wdisplay
    gammastep
    vesktop/discord (if vesktop add theme and plugins manually)
      try to persist only the login file
        Might not work since caching pictures can be large (try to put these in /tmp?)
    mpd
      rmpc
    obs
      make sure to persist obs settings
    games:
      gamemode
      mangohud
      gamemoderun
    radaromega
    video player (not vlc)
    eventually:
      ratbagctl (gui version)
      vial (for keyboard)
      davinci resolve
      gimp
      audacity
      blender
      theme gnome-polkit (or another auth polkit)

Lynis OpenVAS (network) vulnix (system) sbomnix
  nix run github:DeterminateSystems/flake-checker
  nix run nixpkgs#vulnix -- --system
  nix run github:tiiuae/sbomnix#vulnxscan -- .#nixosConfigurations.vmtest.config.system.build.toplevel

add "auto login sunshine" file

edit install.sh to have options for install location and hostname

nvd (nix visual diff, shows differences in packages)
nix-diff (shows differences in files)

mpd
put rmpc into scratchpad on alt+m

pssh (parallel ssh)

auto update:
  store current git commit
  git pull
  nh os switch --update (update gets done on one machine [probably a vm] which gets ran a day early for time to debug issues)
  if failed:
    restore to saved git commit, try nh os switch --update
    if failed:
      send notification through unifiedpush (or discord channel) saying update failed (maybe provide error message)
  if succeeded:
    send notification saying update on hostname succeded



https://github.com/mat-1/metasearch2


test on desktop:
  Confirm flameshot works on multiple monitors
